{
  "name": "agent-template",
  "version": "1.0.0",
  "description": "Claude Agent SDK TypeScript template",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=node",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/agent-sdk": "^0.1.37",
    "zod": "^3.22.4",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
